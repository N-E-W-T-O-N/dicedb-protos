syntax = "proto3";

package wal;
option go_package = "./wal";

enum WALEntryType {
  WAL_ENTRY_TYPE_COMMAND = 0;
}

message WALEntry {
  uint64 lsn = 1;
  uint32 crc32 = 2;
  int64 timestamp = 3;
  WALEntryType entry_type = 4;
  bytes payload = 5;
}
